((nil
  .
  ((eval
    .
    (progn
      (setq-local org-download-heading-lvl nil)
      (setq-local org-download-image-dir (concat default-directory "./static/" (file-name-sans-extension (buffer-name)) "/")))))))
